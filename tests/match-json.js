describe('match-json', function() {
  it('should match json paths for a string');
  it('should match json paths for an object');
  it('should throw when json paths are not matched');
  it('should throw when the json paths are met multiple times but the once flag is set');
});